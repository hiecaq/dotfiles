XDG_CONFIG_HOME DEFAULT=@{HOME}/.config
XDG_DATA_HOME DEFAULT=@{HOME}/.local/share
XDG_CACHE_HOME DEFAULT=@{HOME}/.cache
# editor
VISUAL DEFAULT="emacsclient -c"
EDITOR DEFAULT=${VISUAL}
# Some directories
DOTFILES DEFAULT=@{HOME}/.dotfiles
WORKPATH DEFAULT=@{HOME}/workspace
# languages' dirs
RUSTUP_HOME DEFAULT=@{HOME}/.rustup
CARGO_TARGET_DIR DEFAULT=@{HOME}/.cache/rust-targets
TEXMFHOME DEFAULT=@{HOME}/.config/texmf

# fcitx
GTK_IM_MODULE=fcitx
QT_IM_MODULE=fcitx
XMODIFIERS=@im=fcitx

# vim: ft=conf
